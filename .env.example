# Public site URL (used for canonical, OG, sitemap)
NEXT_PUBLIC_SITE_URL=https://streamquest.io

# Google Tag Manager (loads only after cookie consent if you wire that up)
NEXT_PUBLIC_GTM_ID=GTM-P8H2S8P4

# Add Supabase / form / newsletter keys here later if needed.
# Marketing-only build does not require any of these to run.
